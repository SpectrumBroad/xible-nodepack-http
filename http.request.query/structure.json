{
  "name": "http.request.query",
  "type": "object",
  "description": "Returns the value of the given querystring field from a HTTP(S) request.",
  "inputs": {
    "requests": {
      "type": "http.request"
    }
  },
  "outputs": {
    "values": {
      "type": "string",
      "description": "A single string per request containing the value for the specified querystring parameter name."
    }
  }
}
