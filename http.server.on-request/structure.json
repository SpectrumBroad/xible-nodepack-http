{
  "name": "http.server.on-request",
  "type": "event",
  "description": "Triggered whenever a HTTP(S) request lands on the given route.",
  "inputs": {
    "trigger": {
      "type": "trigger",
      "description": "Starts waiting for a request. You probably want to hook this up to the 'started' output of a 'http.server' or 'http.server.secure' node"
    },
    "servers": {
      "type": "http.server"
    }
  },
  "outputs": {
    "trigger": {
      "type": "trigger"
    },
    "request": {
      "type": "http.request"
    }
  }
}
